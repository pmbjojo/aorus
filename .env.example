# Paperless-NGX Environment Variables
PAPERLESS_USERMAP_GID=GID
PAPERLESS_OCR_LANGUAGE=OCR_LANGUAGE
PAPERLESS_SECRET_KEY=SECURE_SECRET_KEY
PAPERLESS_POSTGRES_DB=PAPERLESS_DB
PAPERLESS_POSTGRES_USER=PAPERLESS_USER
PAPERLESS_POSTGRES_PASSWORD=DB_PASSWORD

# DuckDNS Service
DUCKDNS_SUBDOMAINS=SUBDOMAIN1,SUBDOMAIN2
DUCKDNS_TOKEN=DUCKDNS_TOKEN

# Traefik (example: if using DNS challenge or certificate resolver)
TRAEFIK_EMAIL=EMAIL@EXAMPLE.COM
TRAEFIK_CLOUDFLARE_API_TOKEN=CLOUDFLARE_API_TOKEN

# Teleport (example: replace tokens if needed)
TELEPORT_TOKEN=TELEPORT_JOIN_TOKEN

# Root mount points
STORAGE_ROOT=STORAGE_PATH
CONFIG_ROOT=CONFIG_PATH

# General settings
PUID=PUID
PGID=PGID
TZ=TIME_ZONE